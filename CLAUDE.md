# WebG850 — 作業規律

このリポジトリは PC-G850V / PC-G850VS Web エミュレータの**公開リポジトリ**（実装・仕様書）。

## 規律の継承元

親の `_emulator/G850/CLAUDE.md` の規律（日本語・Sonnet 委譲・ROM 不使用・精度方針）を
そのまま継承する。判断に迷ったらそちらを読むこと。

## 作業ディレクトリについて（重要）

**作業ディレクトリは `_emulator/G850` を指定すること。** `WebG850/` をそのまま cwd にすると
記憶空間が分離し、PC88 等と共有しているエミュレータ共通の記憶が読めなくなる
（詳細は `_emulator/CLAUDE.md`）。

## git 操作について（重要）

**git 操作は必ず `git -C <リポジトリパス>` でリポジトリを明示すること。**
`WebG850/` は `_emulator/G850/` の入れ子リポジトリなので、シェルの cwd に依存すると
親リポジトリ側へ誤コミットする事故が起きる（過去に実績あり）。

## docs/spec/ を編集するとき

`docs/spec/` 配下の YAML を編集・追記するときは、**必ず `docs/spec/SCHEMA.md` を先に読むこと。**
書式定義とテスト仕様を兼ねている。
