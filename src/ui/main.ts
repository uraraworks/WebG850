// エントリポイント。
//
// 【構造の切り出し（テスト検出力の穴を塞ぐための分離）】 DOM 結線本体は
// `setupUi.ts` の `setupUi()` へ切り出した。以前はここに結線処理そのものが
// 書かれていて import しただけで実行されてしまい、結線（`onLoadProgram` の
// 配線等）をテストから通せなかった。この関数はその結線を呼ぶだけの
// エントリポイントに徹する。

import { setupUi } from './setupUi.ts';

setupUi();
