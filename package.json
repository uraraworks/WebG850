{
  "name": "webg850",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "PC-G850V / PC-G850VS 用ブラウザエミュレータ（Phase 1: 互換 BASIC インタプリタ）",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gen": "node tools/gen_command_table.mjs",
    "measure": "vite-node tools/measure_corpus.ts"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/node": "^26.2.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "yaml": "^2.9.0"
  }
}
